local UI = {}

function UI.Settings(Config)
  local Title = Config.Title or "UiLibrary"
  local Theme = (Config.Theme and math.clamp(Config.Theme, 1, 2)) or 1
  print(Title)
  print(Theme)
end
  
return UI
